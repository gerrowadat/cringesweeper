# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: CringeSweeper Dashboards
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards