global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'cringesweeper'
    static_configs:
      - targets: ['cringesweeper:8080']
    scrape_interval: 30s
    metrics_path: /metrics
    
    # Optional: Basic authentication if needed
    # basic_auth:
    #   username: 'prometheus'
    #   password: 'your-password'